import 'package:flutter/material.dart';

class kumpulModul extends StatelessWidget {
  const kumpulModul({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(),
    );
  }
}
